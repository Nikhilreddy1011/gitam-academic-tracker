# Optional — only if the React app is on a *different* host than the API.
# Same server on port 5000: leave unset.
# CRA dev with proxy on :3000: leave unset.
#
# REACT_APP_API_URL=https://your-api.example.com
